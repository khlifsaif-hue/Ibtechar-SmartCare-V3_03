"use client";
import { ArrowLeft, MapPin, Pencil, ShieldCheck, Wrench } from "lucide-react";
import Link from "next/link";
import { useEffect, useState } from "react";
type Asset={id:string;name:string;manufacturer:string;model:string;serialNumber:string;category:string;location:string;project:string;status:string;tone:string;health:number;acquisitionCost:number;residualValue:number;usefulLifeYears:number;purchaseDate:string;warrantyUntil:string;insured:boolean;openBox:boolean;damagePercent:number};
export default function AssetDetailPage({params}:{params:Promise<{id:string}>}){const[asset,setAsset]=useState<Asset|null>(null),[locations,setLocations]=useState<{name:string}[]>([]),[editing,setEditing]=useState(false),[error,setError]=useState("");useEffect(()=>{(async()=>{const{id}=await params;const[a,l]=await Promise.all([fetch(`/api/assets/${id}`).then(r=>r.json()),fetch("/api/locations").then(r=>r.json())]);setAsset(a.asset);setLocations(l.locations||[])})().catch(()=>setError("Unable to load asset."))},[params]);async function save(fd:FormData){if(!asset)return;const payload={...asset,location:String(fd.get("location")),status:String(fd.get("status")),health:Number(fd.get("health")),warrantyUntil:String(fd.get("warrantyUntil")),insured:fd.get("insured")==="on"};const r=await fetch(`/api/assets/${asset.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const d=await r.json();if(r.ok){setAsset(d.asset);setEditing(false)}else setError(d.error||"Unable to update asset.")}if(!asset)return <main className="dashboard-main section-page">Loading asset…</main>;const age=Math.max(0,(Date.now()-new Date(asset.purchaseDate||Date.now()).getTime())/(365.25*86400000));const current=Math.max(asset.residualValue,asset.acquisitionCost-(asset.acquisitionCost-asset.residualValue)*Math.min(1,age/Math.max(1,asset.usefulLifeYears)))*(1-Math.min(0.95,asset.damagePercent/100));return <main className="dashboard-main section-page"><Link className="back-link" href="/assets"><ArrowLeft size={16}/> Back to assets</Link><section className="detail-hero"><div><span className={`status-pill ${asset.tone}`}><i/>{asset.status}</span><h1>{asset.name}</h1><p>{asset.id} · {asset.manufacturer} · {asset.model}</p></div><div className="heading-actions"><Link className="button primary" href={`/work-orders?new=1&asset=${asset.id}`}><Wrench size={17}/> Create work order</Link><button className="button secondary" onClick={()=>setEditing(true)}><Pencil size={16}/> Edit asset</button></div></section>{error&&<p className="form-error">{error}</p>}<section className="detail-kpis"><article><span><small>Health</small><strong>{asset.health}%</strong></span></article><article><span><small>Purchase value</small><strong>QAR {asset.acquisitionCost.toLocaleString()}</strong></span></article><article><span><small>Current value</small><strong>QAR {current.toLocaleString(undefined,{maximumFractionDigits:0})}</strong></span></article><article><span><small>Warranty</small><strong>{asset.warrantyUntil||"Not recorded"}</strong></span></article></section><section className="detail-section"><div className="section-title"><h2>Operational asset record</h2></div><dl className="asset-data-grid"><div><dt>Project</dt><dd>{asset.project}</dd></div><div><dt>Location</dt><dd><MapPin size={14}/>{asset.location}</dd></div><div><dt>Insurance</dt><dd>{asset.insured?"Insured":"Not insured"}</dd></div><div><dt>Condition adjustment</dt><dd>{asset.damagePercent}% damage · {asset.openBox?"Open box":"Sealed at purchase"}</dd></div><div><dt>Serial</dt><dd>{asset.serialNumber}</dd></div><div><dt>Useful life</dt><dd>{asset.usefulLifeYears} years</dd></div></dl></section>{editing&&<div className="modal-layer"><button className="modal-backdrop" onClick={()=>setEditing(false)}/><section className="modal-card activity-modal"><h2>Edit status and location</h2><form action={save}><label>Location<select name="location" defaultValue={asset.location}>{locations.map(l=><option key={l.name}>{l.name}</option>)}</select></label><label>Status<select name="status" defaultValue={asset.status}><option>Operational</option><option>Awaiting commissioning</option><option>Maintenance due</option><option>Out of service</option></select></label><label>Health %<input name="health" type="number" min="0" max="100" defaultValue={asset.health}/></label><label>Warranty until<input name="warrantyUntil" type="date" defaultValue={asset.warrantyUntil}/></label><label className="check-filter"><input name="insured" type="checkbox" defaultChecked={asset.insured}/> Insured</label><button className="button primary full"><ShieldCheck size={16}/> Save asset update</button></form></section></div>}</main>}

